// Configuração pública do Firebase AI Logic.
// Não coloque secrets neste arquivo. O site key do reCAPTCHA Enterprise é público.
// Após registrar o app Web no Firebase App Check, preencha appCheckSiteKey.
window.IDE_MUSIC_AI_CONFIG = Object.freeze({
  enabled: true,
  model: 'gemini-3.6-flash',
  appCheckSiteKey: ''
});
